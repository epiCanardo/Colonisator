namespace Assets.Scripts.Front.StartScene
{
    public class NewGameMenuOption : UnityEngine.MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
